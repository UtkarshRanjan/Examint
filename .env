# This file is read by Prisma CLI (prisma migrate, prisma studio, etc.)
# Next.js reads .env.local instead for runtime variables.
# Both files must define DATABASE_URL so Prisma CLI and the Next.js app agree.
DATABASE_URL="file:./prisma/dev.db"
