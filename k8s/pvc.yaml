apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: examint-data
  namespace: examint
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: standard-rwo
  resources:
    requests:
      storage: 5Gi
